# Codex config snippet produced by /codex-init.
# Review and merge this into ~/.codex/config.toml manually.

# agent-skill:codex-config:start
[hooks]
PreToolUse = [
  { matcher = "^Bash$", hooks = [{ type = "command", command = {{hook_command_pretool_toml}}, command_windows = {{hook_command_pretool_windows_toml}} }] }
]
{{#if mcp_servers_block}}

{{mcp_servers_block}}
{{/if}}
# agent-skill:codex-config:end
