# {{guide_path}} Agent Guide

This folder-level guide was scaffolded by `/codex-init` for {{purpose}}.

## Scope

- This guidance applies when working inside `{{guide_path}}/`.
- Root `AGENTS.md` remains the operational index for project-wide rules, task ledger usage, reviewer personas, and policy expectations.
- Prefer local commands and validation evidence relevant to this folder before broad repo-wide checks.

## Working Rules

- Keep changes scoped to this folder unless the task doc explicitly owns cross-folder work.
- Use `.agent-skill/tasks/NN-slug.md` for task context and keep Progress Snapshot / Verification current. Legacy `docs/tasks/NN-slug.md` remains readable for migration.
- Do not paste large logs here. Store long output in files or context-mode and cite paths from the task doc.
- Commit with explicit pathspecs for files you own.
