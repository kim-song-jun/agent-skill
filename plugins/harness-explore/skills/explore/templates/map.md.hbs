# Codebase map — `{{sha}}`

**Generated:** {{generatedAt}}
**Root:** `{{root}}`
**Size category:** {{sizeCategory}}

## Overview

- **{{totalFiles}}** total files, **{{totalLines}}** total lines
- **{{dirs.length}}** top-level directories scanned

### Language breakdown

{{#each languages}}- `{{this}}`
{{/each}}

## Public entry points

{{#each publicEntryPoints}}- `{{this}}`
{{/each}}

## Top-level directories

{{#each dirs}}
### `{{this.dir}}/`

{{#if this.incomplete}}
**INCOMPLETE** — {{this.reason}}
{{/if}}

- **Files:** {{this.fileCount}} ({{this.totalLines}} lines)
- **Purpose:** {{this.purpose}}

{{#if this.publicEntryPoints}}
**Entry points:**
{{#each this.publicEntryPoints}}- `{{this}}`
{{/each}}
{{/if}}

{{#if this.notableConventions}}
**Conventions:**
{{#each this.notableConventions}}- {{this}}
{{/each}}
{{/if}}

{{#if this.entries}}
**Entries:**
{{#each this.entries}}- `{{this.path}}` — {{this.kind}}{{#if this.lines}} ({{this.lines}} lines){{/if}}{{#if this.exports}} — exports: {{this.exports}}{{/if}}
{{/each}}
{{/if}}

{{/each}}

## Dependency graph

{{#if depGraph.skipped}}
_Skipped — {{depGraph.skipped}}_
{{/if}}

{{#if depGraph.supportedLanguages}}
**Languages scanned:** {{depGraph.supportedLanguages}}

### Orphans (files imported by nothing and not entry points)

{{#each depGraph.orphans}}- `{{this}}`
{{/each}}
{{/if}}

---

Generated by `/explore` (harness-explore). Re-run with `/explore
--force` after non-trivial changes to refresh.
