# Thrift-Codex Audit — {{date}}

**Session duration:** {{durationMinutes}} minutes
**Turns:** {{turnCount}}
**Started:** {{sessionStartedAt}}
**Platform:** Codex CLI ({{codexVersion}})

> **Heuristic note:** Codex does not surface OpenAI cache-hit telemetry
> in v1. `savedRatio` below is a heuristic estimate based on
> `cache.enabled`, session length, and configured priming strategy. The
> CC equivalent of this report uses Anthropic's measured cache-read
> token counts directly.

## Cost summary

| Metric | Value |
|---|---|
| **Actual cost (estimated)** | ${{actualUSD}} |
| **Baseline cost (naive, no-thrift)** | ${{baselineUSD}} |
| **Saved (heuristic)** | ${{savedUSD}} ({{savedPercent}}%) |

{{#each perModelRows}}
- {{this.model}}: {{this.calls}} calls, ${{this.actualUSD}} actual / ${{this.baselineUSD}} baseline
{{/each}}

## Token usage

| Category | Tokens |
|---|---|
| Input (uncached) | {{tokensInUncached}} |
| Input (cached read, estimated) | {{tokensInCached}} |
| Output | {{tokensOut}} |
| **Total input** | {{tokensInTotal}} |

**Cache hit rate (heuristic):** {{cacheHitRate}}%

## Summariser activity

{{#if summariserFires}}
| When | Reason | Tokens before → after | Saved |
|---|---|---|---|
{{#each summarisers}}
| {{this.at}} | {{this.reason}} | {{this.tokensBefore}} → {{this.tokensAfter}} | {{this.savedPercent}}% |
{{/each}}
{{/if}}
{{#if noSummariserFires}}
(none — session shorter than first threshold)
{{/if}}

## Tool coercions (context-mode telemetry)

{{#if coercionFires}}
| When | Tool | Suggestion | Accepted |
|---|---|---|---|
{{#each coercions}}
| {{this.at}} | {{this.tool}} | {{this.suggestion}} | {{this.accepted}} |
{{/each}}

**Coercion acceptance rate:** {{coercionAcceptRate}}%
{{/if}}
{{#if noCoercionFires}}
(none observed — either no large tool outputs, or context-mode-codex unavailable)
{{/if}}

## Cache primes

{{#if cachePrimeFires}}
| When | Cohort | Cost (est.) |
|---|---|---|
{{#each cachePrimes}}
| {{this.at}} | {{this.cohort}} | ${{this.costUSD}} |
{{/each}}

Total prime cost (estimated): ${{primeTotalUSD}}
{{/if}}
{{#if noCachePrimeFires}}
(disabled — `cache.enabled = false` in `.thrift.json`; default on Codex)
{{/if}}

## Phase log

{{#each phases}}
- Phase {{this.phase}} @ {{this.completedAt}}
{{/each}}

---

Generated by the `/thrift` Codex port v{{thriftVersion}} from `.thrift-state.json`.
