# thrift: thrift-sessionend-audit
# Writes Phase 5 audit report at session_end. Always runs (audit always
# matters — even on Ctrl-C the audit hook fires).
[[hooks.session_end]]
command = "node \"{{hooksDir}}/thrift-sessionend-audit.mjs\""
timeout_seconds = 30
# end thrift: thrift-sessionend-audit
