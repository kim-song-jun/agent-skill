# No hard decision-protocol hook snippet is emitted for `/agent-all`.
# Current Codex command hooks do not expose a Task-like subagent tool
# surface for this pipeline. Sequential dispatch must include the
# decision, verification, and reviewer directives in prompt text.
