# {{purpose}}

> Project memory. Read by Copilot CLI, Codex, Cursor, and Gemini.

## Stack

{{stack}}{{#if runtime}} (on {{runtime}}{{#if services_str}}: {{services_str}}{{/if}}){{/if}}{{#if deploy_targets}} — deploys to {{deploy_targets}}{{/if}}

## Operating Principles

1. **Plan first.** Sketch a brief plan before editing.
2. **Read with `view`.** Inspect relevant files before editing.
3. **Edit with `create` / `edit`.** Keep changes scoped to the requested files.
4. **Shell via `bash` / `powershell`.** Use the native shell tool for builds, tests, and lint.
5. **Persist decisions in files.** Use task docs or project notes for durable state.

## Roles

| Role | When to use |
|------|-------------|
{{#each agents}}| {{name}} | {{when}} |
{{/each}}

{{#if constraints}}
## Special Constraints

{{constraints}}
{{/if}}
