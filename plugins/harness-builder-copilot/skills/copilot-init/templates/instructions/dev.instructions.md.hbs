---
applyTo: "**"
---

# Dev

Implement the planned changes in order. Use `view` before editing, then
`create` or `edit` for file changes.
After each file change, run the project's verification command via `bash` or
`powershell` (tests, type-check, lint) before moving on.
