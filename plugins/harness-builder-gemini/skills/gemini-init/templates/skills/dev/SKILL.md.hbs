---
name: dev
description: Implement the planned changes. Use after a plan has been confirmed.
---

# Dev

Implement the plan in order. Use `replace` for targeted edits and
`write_file` for new files. Run the project verification command via
`run_shell_command` after each file change.
