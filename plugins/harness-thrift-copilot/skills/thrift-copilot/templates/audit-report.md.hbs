# Thrift Audit (Copilot) — {{date}}

**Session duration:** {{durationMinutes}} minutes
**Turns:** {{turnCount}}
**Started:** {{sessionStartedAt}}
**Platform:** GitHub Copilot CLI
**Memory adapter:** {{storeMemoryStatus}}

## Cost summary

| Metric | Value |
|---|---|
| **Actual cost (estimated)** | ${{actualUSD}} |
| **Baseline cost (naive, no-thrift)** | ${{baselineUSD}} |
| **Saved (estimated)** | ${{savedUSD}} ({{savedPercent}}%) |

{{#each perModelRows}}
- {{this.model}}: {{this.calls}} calls, ${{this.actualUSD}} actual / ${{this.baselineUSD}} baseline
{{/each}}

> **Intermediation note.** Copilot proxies the underlying model layer.
> Token counts and per-call cost are best-effort estimates derived from
> the configured `summariser.model` and the rate table in
> `lib/cost-estimator.mjs`. Real Copilot billing may differ; verify
> against your GitHub billing dashboard.

## Token usage

| Category | Tokens |
|---|---|
| Input (uncached, estimated) | {{tokensInUncached}} |
| Input (cached read — not measured on Copilot) | {{tokensInCached}} |
| Output (estimated) | {{tokensOut}} |
| **Total input (estimated)** | {{tokensInTotal}} |

**Cache hit rate:** {{cacheHitRate}}% (not measured directly on Copilot)

## Summariser activity

{{#if summariserFires}}
| When | Reason | Tokens before → after | Saved | Mirror |
|---|---|---|---|---|
{{#each summarisers}}
| {{this.at}} | {{this.reason}} | {{this.tokensBefore}} → {{this.tokensAfter}} | {{this.savedPercent}}% | {{this.mirrorStatus}} |
{{/each}}
{{/if}}
{{#if noSummariserFires}}
(none — session shorter than first threshold)
{{/if}}

## Tool coercions (context-mode-copilot telemetry)

{{#if coercionFires}}
| When | Tool | Suggestion | Accepted |
|---|---|---|---|
{{#each coercions}}
| {{this.at}} | {{this.tool}} | {{this.suggestion}} | {{this.accepted}} |
{{/each}}

**Coercion acceptance rate:** {{coercionAcceptRate}}%
{{/if}}
{{#if noCoercionFires}}
(none observed — either no large tool outputs, or context-mode-copilot unavailable)
{{/if}}

## Cache primes

{{#if cachePrimeFires}}
| When | Cohort | Cost (est) |
|---|---|---|
{{#each cachePrimes}}
| {{this.at}} | {{this.cohort}} | ${{this.costUSD}} |
{{/each}}

Total prime cost (estimated): ${{primeTotalUSD}}
{{/if}}
{{#if noCachePrimeFires}}
(disabled — Phase 4 defaults to off on Copilot; see
`phases/4-cache-prime.md`)
{{/if}}

## Phase log

{{#each phases}}
- Phase {{this.phase}} @ {{this.completedAt}}
{{/each}}

---

Generated by `harness-thrift-copilot` v{{thriftVersion}} from
`.thrift-state.json` (plus optional memory-adapter mirror, if enabled).
