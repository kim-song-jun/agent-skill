# Thrift Recap — {{date}} (Cursor)

**Session duration:** {{durationMinutes}} minutes
**Turns:** {{turnCount}}
**Started:** {{sessionStartedAt}}

> Cursor does not surface per-turn token counts or cost. Numeric fields
> below are best-effort — fill them from Cursor's usage panel if you
> want a quantified recap, otherwise leave the `n/a` markers in place.

## Cost summary (advisory only)

| Metric | Value |
|---|---|
| **Actual cost** | ${{actualUSD}} |
| **Baseline cost (naive, no-thrift)** | ${{baselineUSD}} |
| **Saved** | ${{savedUSD}} ({{savedPercent}}%) |

{{#each perModelRows}}
- {{this.model}}: {{this.calls}} calls, ${{this.actualUSD}} actual / ${{this.baselineUSD}} baseline
{{/each}}

## Token usage (advisory only)

| Category | Tokens |
|---|---|
| Input (uncached) | {{tokensInUncached}} |
| Input (cached read) | {{tokensInCached}} |
| Output | {{tokensOut}} |
| **Total input** | {{tokensInTotal}} |

**Cache hit rate:** {{cacheHitRate}}%

## Summariser activity

{{#if summariserFires}}
| When | Reason | Tokens before → after | Saved |
|---|---|---|---|
{{#each summarisers}}
| {{this.at}} | {{this.reason}} | {{this.tokensBefore}} → {{this.tokensAfter}} | {{this.savedPercent}}% |
{{/each}}
{{/if}}
{{#if noSummariserFires}}
(none — session shorter than first threshold, or planner did not suggest one)
{{/if}}

## Tool coercions (advisory)

{{#if coercionFires}}
| When | Tool | Suggestion | Accepted |
|---|---|---|---|
{{#each coercions}}
| {{this.at}} | {{this.tool}} | {{this.suggestion}} | {{this.accepted}} |
{{/each}}
{{/if}}
{{#if noCoercionFires}}
(none recorded — Cursor cannot capture tool-call telemetry; relies on
planner self-report)
{{/if}}

## What happened this session (narrative)

{{#if narrative}}
{{narrative}}
{{/if}}
{{#if noNarrative}}
_Fill in: attempted goals, files touched, what worked, what didn't,
outstanding TODOs._
{{/if}}

## Notes

- Cache prime phase: **n/a** (Cursor exposes no cache surface; phase
  removed from this port).
- State file: **n/a** (Cursor does not produce `.thrift-state.json`).
- This recap was generated from the planner's narrative + any
  user-pasted numbers; it is not a programmatic measurement.

---

Generated by `harness-thrift-cursor` v{{thriftVersion}} (Cursor port —
advisory only).
