# v0.7.2 Adversarial Remediation Plan + Ledger

> Durable execution ledger (survives compaction). Source of truth = the 2nd adversarial round (Workflow, 42 agents) + 4 read-only investigations (2026-06-22). User-approved decisions: **fix ALL 7 defects → v0.7.2**; Gemini = **honest downgrade**; C2/C4 = **real executable gate**.

**Global constraints:** shared worktree → sequential edits only, NO git stash/reset/checkout-revert/clean/add-A; commit only my files via pathspec + `git show --stat` verify. sync-lib vendored files (gate-plan, memory-bridge, wiki-index, audit-tokens, NEW gate-check) must pass `sync-lib --check` (byte-identical). Phase `.md` + `agent-all.config.json.hbs` templates are HAND-AUTHORED (not sync-lib-managed) → edit directly per port. No pass-only tests — every new test must fail if the behavior regresses.

## Slices (each gets independent opus adversarial verification)

### Slice A — [ ] Copilot wrong import path (CRITICAL #1)
Edit 6 imports `./lib/` → `./.copilot/agent-all/lib/`:
- `harness-floor-copilot/.../phases/0-preflight.md:22` makeFileMirror, `:23` recallLatestCheckpoint
- `.../phases/3-dispatch.md:51` makeFileMirror, `:52` flushCheckpoint
- `.../phases/4-gate.md:27` buildGatePlan
- `.../phases/5-pr.md:14` validateTaskLedger
(4-gate.md:187 already correct = pattern.) + NEW regression test: assert no copilot phase-doc `import ... from "./lib/"` bare path; each imported file must exist under the copilot lib source tree (catches doc-vs-install drift).

### Slice B — [ ] quality-debt gate on Cursor/Codex (IMPORTANT #6)
Insert quality-debt-reviewer pass bullet:
- cursor `phases/4-gate.md` §5b: after line 46 (`required \`qa-reviewer\`, AND`), before line 47.
- codex `phases/4-gate.md` §4b: after line 78 (`returned \`qa-reviewer\`, AND`), before line 79.
Bullet text (match port idiom): "`quality-debt-reviewer` reports no unapproved quality debt; every accepted exception must be recorded in the task doc `Quality Debt Exceptions` table with reason, owner, follow-up issue, and expiry, AND". + optional doc-contract test: every port 4-gate.md includes the quality-debt pass condition.

### Slice C — [ ] Gemini honest downgrade (CRITICAL #2+#3)
- `harness-floor-gemini/.../templates/agent-all.config.json.hbs` gates block: ADD `"adversarialVerify": false` (verify qualityReview spelling first). NOT sync-lib-managed → safe. (Do NOT edit gemini gate-plan.mjs — byte-vendored, breaks --check.)
- gemini `phases/4-gate.md` `## Gemini-specific` (~L128-138): add note adversarial gate unsupported (subprocess model can't give structural isolation).
- gemini `phases/0-preflight.md:21`: note `--resume` only skips Phase 0 re-init, does NOT restore handoff artifacts / checkpoint memory (resume-artifacts.mjs + memory-bridge.mjs are CC-only).

### Slice D — [ ] Executable adversarial gate (IMPORTANT #4, biggest)
- NEW source `harness-floor/skills/agent-all/lib/policy/gate-check.mjs`: reads stdin/`$GATE_VERDICT_TEXT`, calls `adversarialAuditBlocks()`, exit 2 if blocked (failed), else exit 0. Imports `./audit-tokens.mjs`.
- Register in `scripts/sync-lib.mjs`: GATE_CHECK_SOURCE + GATE_CHECK_TARGETS (codex/copilot/cursor — ports that RUN adversarial gate AND have lib/policy/audit-tokens.mjs; NOT gemini = disabled) + drift loop + totalChecked.
- `sync-lib` write → vendor.
- Replace prose code-fence in 4-gate.md (CC + copilot + codex + cursor) with deterministic shell: `printf '%s' "$ADV_AUDIT_TEXT" | node --input-type=module <port-correct-path>/lib/policy/gate-check.mjs` (non-zero exit = block). Avoid cwd-dangle: copilot=`.copilot/agent-all/lib/...`, cursor=`.cursor/agent-all/lib/...`, codex=`.codex/skills/agent-all/lib/...`, CC=skill-dir-resolved.
- NEW test gate-check.mjs: failed→exit2, passed/skipped/missing→exit0.
- Honest claim language: SKILL.md/docs say "LLM-runs a deterministic node gate (block decision is code, exit-coded), not a runtime hook" — NOT "machine gate enforced regardless of LLM".

### Slice E — [ ] wiki compile guard (IMPORTANT #5)
- `harness-floor/skills/wiki/lib/wiki-index.mjs` `compileSelfAudit` (L142): guard — if `!existsSync(absDir)` → `{ok:false, missing:"directory"}`; if no INDEX.md → `{ok:false, missing:"INDEX.md"}`. Valid-empty wiki (INDEX.md present, 0 rows) still ok:true.
- sync-lib write → propagate to codex wiki copy (`harness-floor-codex/skills/wiki-codex/lib/wiki-index.mjs`).
- Test: compile nonexistent→exit1, no-INDEX→exit1, valid-empty→exit0, tampered→exit1.

### Slice F — [ ] atomic checkpoint write (MINOR #7)
- SOURCE = `harness-floor-copilot/.../agent-all-copilot/lib/memory-bridge.mjs` write() (L45-50): tmp+`renameSync` (add renameSync import).
- Patch orphan `harness-floor-copilot/skills/visual-qa-copilot/lib/memory-bridge.mjs` (NOT sync-tracked) identically.
- `recallLatestCheckpoint` (memory-agent.mjs): fallback to newest per-wave checkpoint if LATEST fails to parse.
- sync-lib write → propagate to CC/codex/cursor.
- Test: corrupt/truncate LATEST → recall recovers via fallback.

## Release (controller, sequential, gated) — [ ]
- sync-lib --check green; full suite green (note new tests).
- Bump 0.7.1→0.7.2: 23 plugin.json + 3 gemini-extension.json + README.md:879 (×3) + README.ko.md:870 (×3) + generate-release-user-manual.mjs L7,8,12,78,80 + thrift-codex.test.mjs:25,28 + thrift-cursor.test.mjs:48 + release-candidate.test.mjs:74,75 + CHANGELOG.md + CHANGELOG.ko.md. + README tests-badge count if test count changed.
- Regen user manual; run all release gates + live-CLI; provenance.
- Pathspec commit → `git show --stat` → push origin main → tag v0.7.2 + rc-<date>.
- 3-CLI reinstall: Claude update.sh, Codex update-codex-plugins.sh, Copilot global SKILL.md.

## Progress
- Slice A: DONE — copilot(6)+codex(5)+cursor(1)=12 imports → install-anchored; +E6 regression (port-ssot). Re-audit found codex/cursor had the SAME bug (workflow only probed copilot). Proven not-pass-only.
- Slice B: DONE — quality-debt pass-condition added to cursor §5b + codex §4b; +E7 regression. port-ssot 22→26.
- Slice C: DONE — gemini config `adversarialVerify:false` (buildGatePlan no longer emits adversarial dispatch, probe-confirmed) + 4-gate.md/0-preflight honest notes (adversarial unsupported, lib not installed, --resume no checkpoint recall); +4 gemini-honest-downgrade tests. (gemini gate-plan NOT edited — byte-vendored.)
- Slice D: DONE — NEW lib/policy/gate-check.mjs (exit 2 on failed, probe-confirmed all verdicts + vendored copies) + sync-lib register (audit-tokens→copilot/cursor, gate-check→codex/copilot/cursor; --check 196→201) + 4 ports' 4-gate.md wire deterministic invocation + SKILL.md honest language; +gate-check.test.mjs(4) +E8 port-ssot.
- Slice E: DONE — wiki-index.mjs compileSelfAudit guard (missing dir/INDEX.md → exit 1; valid-empty → 0; probe-confirmed) + CLI missing-reason; synced to codex wiki; +3 wiki-compile-gate tests + empty-valid fixture.
- Slice F: DONE — memory-bridge write() atomic (tmp+rename, source=copilot) + listKeys; memory-agent recallLatestCheckpoint fallback to newest per-wave on corrupt LATEST (source=CC — initially edited wrong copy, sync overwrote it, redone on CC source); orphan visual-qa-copilot patched; +4 checkpoint-atomic-recovery tests (corrupt→recover probe-confirmed). synced.
- Release-tax fix: release-smoke-script.test.mjs focused count 601→604 (+3 wiki) + 601 banned.
- FULL SUITE: 2231/2231/0/0 green (was 2205, +26). sync-lib --check 201 green.
- ADVERSARIAL ROUND 1 (Workflow, 6 opus adversaries + synth): 0 refuted core claims; B/D/F fully clean; 7 probe-backed RESIDUALS found. All independently re-confirmed by me.
- ROUND-2 FIXES (residuals):
  - R2-A: visual-qa-codex had 5 bare ./lib/ (SAME class, codex installs to .codex/skills/visual-qa/lib/) — anchored all 5; REPLACED E6 with comprehensive INSTALL_ANCHOR_SCAN (agent-all + visual-qa × codex/copilot/cursor = 6 guards). gemini agent-all bare ./lib/ = reference-logic (no lib install), left + framed via C.
  - R2-E: wiki parseIndex silently dropped malformed declared-page rows (invalid grade) → vacuous diff=0 (probe: grade-X ghost row exit0 vs grade-A exit1). Fix: parseIndexRows returns {entries, malformed}; compileSelfAudit ok:false + surfaces malformed; parseIndexRaw stays array wrapper. + statSafe().isFile() guard (INDEX.md-as-dir was EISDIR crash). +3 tests + malformed-row fixture.
  - R2-C: gemini 4-gate.md note contradicted siblings ("vendored — use it") + listed non-existent "memory" + SKILL advertised /agent-handoff (not bundled). Fix: note distinguishes plugin-vendored vs project-copied + reconciles "vendored—use it" (follow logic) + dropped "memory" (now break-resolver/verification-adapters) + SKILL handoff port-note. +tests.
  - release-tax: focused count 604→607 (+3 wiki); 604 banned.
  - FULL SUITE 2238/2238/0/0 green (2205→2238, +33). sync-lib --check 201 green.
- SURFACED (NOT auto-fixed, rule 13 — separate plugin, out of v0.7.2 scope): harness-debug-codex/debug-codex/phases/5-summarise.md:20 has the SAME bare ./lib/ bug (installs to .codex/skills/debug/). 1-line fix. → ASK user whether to include in v0.7.2.
- ROUND-2 RE-VERIFICATION (Workflow, 3 adversaries + completeness critic): R2-A/R2-C clean; found 3 MORE (all I reproduced):
  - **CRITICAL**: documented gate-check command `node --input-type=module <file>/gate-check.mjs` CRASHES (ERR_INPUT_TYPE_NOT_ALLOWED exit 1 on Node v24 — flag only valid with -e/stdin, not a file path). My probe+test used flag-free form → green while DOCUMENTED command broken. THIRD time the "didn't run the LITERAL documented command" trap bit.
  - IMPORTANT: gate-check.test.mjs was pass-only vs the documented contract (ran flag-free).
  - IMPORTANT: wiki parseIndexRows `if(!linkMatch) continue` drops link-malformed page rows ([Paren](file(1).md), [Empty](), bare page.md) BEFORE malformed[] → vacuous diff=0 still leaked.
- ROUND-3 FIXES: removed --input-type=module from gate-check file-path invocations (4 phase docs + header); doc-contract test now install-SIMULATES + runs the LITERAL per-port command asserting exit 2 (would catch the flag bug); parseIndexRows flags looksLikePageDecl (`](` or *.md) rows as malformed before continue; +CLI null-file handling. FULL SUITE 2238→2244 green, focused 607→609, sync-lib 201.
- ROUND-3 RE-VERIFICATION (Workflow, in flight): gate-check-command + wiki-link-malformed + convergence critic.
- LESSON (now 3×): the "run the LITERAL documented command/input the operator emits, never a paraphrase" rule is the single highest-value verification discipline — a paraphrased probe shipped 3 defects (copilot ./lib path, gate-check --input-type flag). Doc-contract tests that execute the literal phase-doc string (install-simulated) are the guard.
- NEXT: round-3 re-verify converged → debug-codex decision → v0.7.2 release (bump 23 plugin.json + 3 gemini-ext + README badge 2205→2244 + manual + tests 0.7.1→0.7.2) + 3-CLI reinstall.
